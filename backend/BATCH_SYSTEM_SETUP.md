# 🚀 Batch Processing System для РСЯ Cleaner

## 📊 Архитектура

Система обрабатывает 100k проектов/день (3 раза в день) через батчи:

```
[Scheduler (CRON)] → [Message Queue] → [Batch Workers (10-50)] → [DLQ Processor]
```

---

## ✅ Развёрнутые функции

| Функция | URL | Описание |
|---------|-----|----------|
| **rsya-scheduler** | `https://functions.yandexcloud.net/d4e5amqqsd544qaf39ls` | Планировщик (каждый час) |
| **rsya-batch-worker** | `https://functions.yandexcloud.net/d4eq5hst4mn9mmcttib1` | Обработчик батчей |
| **rsya-dlq-processor** | (см. func2url при наличии) | Повторная обработка ошибок |
| **rsya-async-poller** | `https://functions.yandexcloud.net/d4emv493feplf9hvgkb4` | Поллер async отчётов |

---

## 📋 Таблицы БД (созданы)

### `rsya_project_schedule`
Расписание запуска проектов (3 раза/день = каждые 8 часов):
- `project_id` — ID проекта
- `interval_hours` — интервал запуска (по умолчанию 8)
- `next_run_at` — время следующего запуска
- `is_active` — активность

### `rsya_campaign_batches`
Батчи кампаний для обработки:
- `project_id` — ID проекта
- `campaign_ids` — JSON массив ID кампаний
- `batch_number` / `total_batches` — номер батча
- `status` — pending / processing / completed / failed
- `retry_count` — количество попыток

### `rsya_campaign_locks`
Блокировки кампаний (избегаем race condition):
- `campaign_id` — ID кампании
- `locked_by` — кто заблокировал (request_id)
- `expires_at` — время истечения блокировки

---

## 🔧 Настройка CRON (ОБЯЗАТЕЛЬНО!)

### 1. Scheduler (каждый час)
Сканирует проекты, создаёт батчи, отправляет в MQ:
```bash
0 * * * * curl -X GET https://functions.yandexcloud.net/d4e5amqqsd544qaf39ls
```

### 2. Report Poller (каждые 5 минут)
Обрабатывает async отчёты (201/202):
```bash
*/5 * * * * curl -X GET https://functions.yandexcloud.net/d4emv493feplf9hvgkb4
```

### 3. DLQ Processor (каждый час)
Повторно обрабатывает failed батчи:
```bash
0 * * * * curl -X GET https://functions.yandexcloud.net/d4e5amqqsd544qaf39ls
```

---

## 🚀 Первый запуск

### Шаг 1: Добавить проекты в расписание
```sql
INSERT INTO rsya_project_schedule (project_id, interval_hours, next_run_at, is_active)
SELECT 
    id, 
    8, 
    NOW(), 
    TRUE
FROM rsya_projects
WHERE yandex_token IS NOT NULL
ON CONFLICT (project_id) DO NOTHING;
```

### Шаг 2: Создать Message Queue (если ещё нет)
Имя очереди: `rsya-batches`
URL: `https://message-queue.api.cloud.yandex.net/b1gtcrip05he61994ldo/dj600000007lh09q06il/rsya-batches`

**⚠️ ВАЖНО:** Обновить queue_url в функциях если URL другой!

### Шаг 3: Запустить Scheduler вручную
```bash
curl -X GET https://functions.yandexcloud.net/d4e5amqqsd544qaf39ls
```

Проверить результат:
```sql
SELECT COUNT(*), status FROM rsya_campaign_batches GROUP BY status;
```

Должно появиться `pending` батчей.

### Шаг 4: Настроить триггер на Message Queue
Batch Worker должен автоматически вызываться при новых сообщениях в MQ.

**Yandex Cloud:** Functions → Triggers → Create Trigger:
- Type: Message Queue
- Queue: `rsya-batches`
- Function: `rsya-batch-worker`
- Batch size: 1 (обрабатываем по одному батчу)

---

## 📊 Мониторинг

### Проверить статус батчей
```sql
SELECT 
    status, 
    COUNT(*) as count,
    AVG(processing_time_sec) as avg_time
FROM rsya_campaign_batches
WHERE created_at > NOW() - INTERVAL '1 hour'
GROUP BY status;
```

### Найти проблемные батчи
```sql
SELECT 
    id, 
    project_id, 
    status, 
    retry_count, 
    error_message
FROM rsya_campaign_batches
WHERE status = 'failed'
ORDER BY created_at DESC
LIMIT 20;
```

### Проверить блокировки (race condition)
```sql
SELECT * FROM rsya_campaign_locks WHERE expires_at > NOW();
```

---

## 🎯 Как это работает

### 1. Scheduler запускается каждый час:
- Сканирует `rsya_project_schedule WHERE next_run_at <= NOW()`
- Для каждого проекта:
  - Получает campaign_ids (например, 100 кампаний)
  - Разбивает на батчи по 14 кампаний (≈ 14 × 15 сек = 210 сек < 300 сек timeout)
  - Создаёт записи в `rsya_campaign_batches`
  - Отправляет каждый батч в Message Queue
- Обновляет `next_run_at = NOW() + 8 hours`

### 2. Batch Worker обрабатывает батч:
- Получает сообщение из MQ (триггер)
- Для КАЖДОЙ кампании в батче:
  1. **Блокирует кампанию** (avoid race condition)
  2. **Получает площадки за 3 периода:**
     - Сегодня (0-0 дней назад)
     - Вчера (1-1 день назад)
     - 7 дней (7-0 дней назад)
  3. **Если отчёт async (201/202):**
     - Сохраняет в `rsya_pending_reports`
     - Пропускает кампанию (поллер обработает)
  4. **Если отчёт ready (200):**
     - Парсит TSV
     - Объединяет площадки, убирает дубли
     - Получает blocked sites
     - Фильтрует (убирает уже заблокированные)
     - **Ротация:** если > 1000 → удаляет наименее дорогие
     - Блокирует новые площадки
  5. **Снимает блокировку кампании**

### 3. Report Poller (каждые 5 минут):
- Получает pending отчёты из `rsya_pending_reports`
- Запрашивает у Яндекса (повторно)
- Если готово (200) → парсит → обрабатывает → триггерит automation

### 4. DLQ Processor (каждый час):
- Находит failed батчи (`status = 'failed'`)
- Отправляет обратно в Message Queue
- Инкрементирует `retry_count`
- Максимум 3 попытки

---

## ⚙️ Параметры батчей

| Параметр | Значение | Комментарий |
|----------|----------|-------------|
| **BATCH_SIZE** | 14 кампаний | 14 × 15 сек = 210 сек (безопасно) |
| **SAFE_TIMEOUT** | 210 сек | 70% от 300 сек (Cloud Function) |
| **AVG_TIME_PER_CAMPAIGN** | 15 сек | Эмпирическое значение |
| **MAX_WAIT_FOR_429** | 60 сек | Максимум ждём при rate limit |
| **RETRY_DELAYS** | [5, 10, 20, 40, 60] | Exponential backoff |

---

## 🔥 Частые проблемы

### 1. Батчи висят в `pending`
**Причина:** Триггер на MQ не настроен  
**Решение:** Создать триггер (см. Шаг 4)

### 2. Много failed батчей
**Причина:** 429 Rate Limit, timeout API  
**Решение:** DLQ processor автоматически retry

### 3. Race condition (дубли блокировок)
**Причина:** Несколько воркеров обрабатывают одну кампанию  
**Решение:** `rsya_campaign_locks` таблица (уже работает)

### 4. Async отчёты не обрабатываются
**Причина:** Поллер не запущен  
**Решение:** Добавить CRON для поллера (каждые 5 минут)

---

## 📈 Масштабирование

### Текущая производительность:
- **100k проектов/день**
- **Среднее кампаний/проект:** 20
- **Батчей:** 100k × 20 / 14 ≈ **150k батчей**
- **Обработка:** 150k / 24 часа ≈ **1700 батчей/час**
- **Воркеров:** 1700 × 120 сек / 3600 ≈ **60 параллельных**

### Если нужно больше:
1. Уменьшить `BATCH_SIZE` (10 вместо 14)
2. Увеличить интервал (12 часов вместо 8)
3. Добавить приоритеты проектам (VIP обрабатываются первыми)

---

## ✅ Чеклист MVP

- [x] Таблицы созданы
- [x] Функции задеплоены
- [x] Тесты пройдены
- [ ] CRON настроен (3 задачи)
- [ ] Message Queue триггер настроен
- [ ] Добавлены проекты в schedule
- [ ] Запущен первый тест (100 проектов)
- [ ] Мониторинг настроен (алерты в Telegram)

---

## 🎯 MVP ГОТОВ!

Осталось только:
1. Настроить 3 CRON-задачи (см. выше)
2. Настроить триггер MQ → Batch Worker
3. Добавить проекты в расписание (SQL запрос)
4. Запустить Scheduler вручную
5. Мониторить батчи в БД

**После этого система автоматически обработает 100k проектов 3 раза в день!** 🚀
